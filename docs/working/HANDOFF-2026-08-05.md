# HANDOFF — 2026-08-05 — cold-start entry point

**Fresh session: read this, then HANDOFF-2026-08-04.md for deeper
context.** Owner: Lara (she/her). Working rules in auto-memory
(`collaboration-checkins-subagent-economics`): believe her explicit
statements (she reads visible thinking — don't theorize past what she
says), chitchat decisions, codex ~free, subagents for parcelable work
(sonnet cheap / opus heavy).

## Track A: Pin & Paper — DESK OBJECTS DRAWER ERA, all pushed

Branch `claude/drag-drop-canvas-mvp-cu6uoy` (pin-and-paper +
pin_and_paper_canvas; card_renderer/sketchpad untouched today). Shipped
across the 08-04 evening → 08-05 early-AM session, all owner-verified
on device unless noted:

- **Desk-objects tab drawer** (right edge, near top): amethyst +
  citrine/rose-quartz/fluorite hue-shift variants + dachshund. Ghosted
  if placed; tap-available → returns to its LAST desk spot + focus pan;
  tap-ghosted → find-my-object (fixed: was a single-ticker bug — canvas
  now TickerProviderStateMixin; focus also restores 1:1 zoom when
  zoomed out). Put-away chip on selected objects. DB v15 `desk_objects`
  (sync-silent), legacy amethyst prefs migrate on first write.
- **Dachshund**: final_v2_widened sprites (frame 1.75×, shadows never
  clip; dog pixel-identical to approved v1). Box default 392 (=owner's
  4-click size bump), clamps 112–1176. 7-stop double-tap turntable.
- **Silhouette hit-testing** (owner: "BEAUTIFUL"): canvas
  `entityHitTest` gate (sits ABOVE lift wrappers — DecoratedBox
  swallows rejections otherwise); dachshund = per-stop 96px alpha mask
  of the color layer (shadows deliberately not grabbable), crystals =
  projected-hull test (`amethystChunkContainsPoint`). Selected objects
  keep the whole box (chips live in the margin).
- **Done pile**: count 8, outline box + RECENTLY COMPLETED label
  (mirrors the NEW tray), felt-tap in box fans down the right edge
  (stops short of the tray), ANY tap off the fanned cards collapses
  (felt or other entity; tapping a fanned card inspects). Tray stack
  now centers its fan ENVELOPE in its box.
- Desk-object defaults 4 clicks bigger (amethyst 262×209.6, clamps
  90–490).

**Owner device-pass queue:** everything above is verified except the
tray-envelope centering + fan-bottom-limit push (`e9ebf51`) — she was
mid-testing at session end.

**Queued/roadmap (owner re-confirmed):** selection/z-order project
(move-to-front etc. — SELECTION_UX + ZORDER research docs), relational
strings, perspective A/B, back-fields settings UI, live canvas updates,
Supabase sync for canvas+drawings, manila-folder workspace, gobo
lighting. NEW: viewport/first-open framing — codex research LANDED at
`DESK_VIEWPORT_RESEARCH.md` (headline: one 2000×1500 desk, adapt the
CAMERA per form factor; owner was reading it at session end, decisions
pending).

## THE REAL DESK ERA (late 08-05 session, owner-sourced art)

All pushed on the working branch; owner verified on device except where
noted:

- **Void backdrop:** owner's top-down FLOOR.png as screen-space
  cover-crop backdrop (`desk_void_backdrop.jpg`), replacing flat black.
- **The desk is a REAL desk:** owner's executive-desk render
  (DESKTRANS.png, background-removed by her) perspective-squared so the
  top slab maps to a rect; drawers hang below the canvas as decoration
  (canvas + background Stacks are Clip.none). Lip leveled (~0.5° roll,
  fixed by ramped per-column shift of the VISIBLE gradient-fit edge —
  alpha measurements lie, see pipeline doc), K=1.0 full de-shine
  (owner: "I love k1").
- **Canvas REBOUND to the desk's inner bevel panel:**
  `kCanvasScreenSize` = **1823×1264** (aspect 1.442). Desk keeps true
  proportions; the CANVAS adapts (owner call — do NOT warp the desk to
  4:3). Canvas origin = panel top-left = asset px (154,187); image
  offset (-154,-187), asset 2128×2264 @1:1.
- **DESK MATS (planned):** full-canvas art, 1823×1264 logical (make
  2× = 3646×2528), square corners — drops exactly into the bevel.
- **PIPELINE + SOURCES preserved** at
  `docs/working/reference/desk_assets/` (regenerate_desk_asset.py +
  DESKTRANS/FLOOR/DESK pngs) — regenerating the app asset is one
  script run; all geometry constants documented in its docstring.
- **OPEN BUG (owner screenshot, end of session): bounding area is off**
  — entities/zone furniture render offset relative to the desk image
  (pile/tray appear too high, cards overlap bevel). Remote-forensics
  was inconclusive; next step is a runtime layout probe (compare the
  desk Image's paint offset vs an entity at canvas origin) rather than
  screenshot pixel-math. NOTE also stored positions from the
  2000×1500-era can legitimately sit outside the new 1823×1264 bounds
  (one-time migration quirk — drag them in) — distinguish that from the
  real bug.
- Owner musing (unconfirmed): the desk might want to be a bit wider
  someday.
- kraft paper texture retired from Spatial View but still in assets.
- **BOUNDING "BUG" RESOLVED:** it was pre-rebind stored positions, not
  a layout bug (owner confirmed after dragging edge cards in). Two
  fully-stranded cards were freed by direct DB update — the LIVE
  desktop DB is `~/Documents/pin_and_paper.db` (v15, 318 tasks); the
  one under `~/.local/share/pin_and_paper/` is a stale v6. "Recall all
  cards" (confirm + undo) added to FEATURE_REQUESTS.
- **NEXT BIG PROJECT SCOPED: MODEL THE DESK IN BLENDER** — full brief
  at `DESK_3D_BRIEF.md` (hinged "bent set" architecture, owner's
  invention; ortho camera; panel-rect by construction; reskins/rigged
  drawers/gobo compliance). **Sequence (owner): gems habit round
  finishes → owner approves strip → farm-render full gem bundles →
  THEN clear context and kick off the desk project in a fresh
  session.**

## Track B: Blender workshop

- **Dachshund final_v2_widened** shipped + integrated (see Track A).
  Workshop repo commit `ae9d03c` (LOCAL ONLY — repo has no remote).
- **Gem cluster ("gemchunk") final_v1 bundle DONE + owner-reviewed**
  (commit caf4cfe; rose quartz reworked milky/waxy per her spec —
  measured, not vibes; variant_strip.png). Owner verdict: great but
  superseded by the HABIT ROUND (2026-08-05): each mineral should grow
  like its real self — amethyst + citrine as DIFFERENTLY-ARRANGED
  prismatic clusters, rose quartz massive/anhedral (keep approved
  material), fluorite as interpenetrating CUBES, plus NEW snowflake
  obsidian (black conchoidal glass, grey rosette patches in shader);
  internal seams/cloudy occlusions inside translucent crystals +
  per-face color variation. A workshop agent is iterating to a
  5-mineral review strip (assets/gemchunk/renders/habit_strip_v1.png)
  — STOPS there; owner approves looks BEFORE full bundles render. If
  it finished, surface the strip; app integration afterwards follows
  the dachshund pattern (sprites into canvas module, entities + drawer
  tiles + alpha hit masks, replacing the painted CustomPainter stones).

## Infrastructure notes

- Full app test suite has KNOWN environmental failures on
  MysteryOfGlass (quickjs .so missing → all date-parsing tests; <1ms
  perf flake; widget_test AppBar overflow — all pre-existing, verified
  on clean tree). Judge regressions against everything else.
- Under parallel/heavy load the suite throws extra timing flakes —
  rerun suspicious files in isolation before believing them.
- `flutter analyze`/`flutter test` MUST run from the app subdir
  (`pin-and-paper/pin_and_paper/`) — the Bash cwd resets between
  calls; running from `~/Documents/Git` analyzes every repo (17k
  phantom errors).
