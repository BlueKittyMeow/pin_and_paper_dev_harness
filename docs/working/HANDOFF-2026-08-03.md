# HANDOFF — Pin & Paper marathon session, 2026-08-02/03

**Purpose:** failsafe for context loss. Everything a fresh session needs.
**Read first:** `DRAG_DROP_CANVAS_MVP_PLAN.md` (plan of record) +
`pin-and-paper/docs/specs/spatial-view-m3-m4-addendum.md` (interrogated,
wins on conflict) + auto-memory `pin-and-paper-plan-of-record`.

## Shipped & pushed (all verified green unless noted)

- **Main app (`pin-and-paper`, `main`):** top-insert fix (MIN−1 positions,
  `28d7d1e`), fast launch (`e658f35`), flaky-test drain fix (`67145b0`,
  authored by chip session), CORE_API refresh, all specs/docs.
- **Main app (branch `claude/drag-drop-canvas-mvp-cu6uoy`):**
  M3 = DB v13 `canvas_x/canvas_y` + `updateTaskCanvasPosition` (`bae5bc7`,
  verified 65/65, PUSHED). M4 = full Spatial View (`f8348e5`, committed
  locally, **NOT yet pushed — full test batch was still running in
  background at handoff time; verify then push**). M4 contents: lib/spatial/
  (TaskSpatialEntity zIndex=−position, TaskSpatialDataSource with LANDING
  TRAY for null-position tasks, task_card_adapter, spatial_desk_background),
  canvas_screen (strict isLoading guard), home AppBar button, extracted
  public `isTaskOverdue` in date_parsing_service.dart (both legacy private
  copies repointed), kraft asset, tests in test/spatial/, test/screens/,
  test/services/task_overdue_helper_test.dart.
- **Canvas module (`pin_and_paper_canvas`, branch `claude/…`):** M1 +
  hit-test fix + UX round (raise/trackpad/background) + drag-select +
  kraft example + flip wiring + amethyst desk object + resize chips +
  liftDecorationBuilder + persistence (positions/size/flips/back-fields via
  shared_preferences) + Card-backs labeled menu. Pushed through `7d0b00a`.
- **Card renderer (branch `claude/…`):** M2 TaskCard + notecard texture +
  flip (TaskCardBack/FlippableTaskCard/TaskCardBackFields) + down-left
  shadows. Pushed through `91af84b`.
- **Android APK** with both app fixes:
  `pin_and_paper/build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`.

## IN FLIGHT at handoff (uncommitted / running)

1. **Amethyst ground-shadow saga** — canvas example, UNCOMMITTED working
   tree changes in `pin_and_paper_canvas` (main worktree): one-field
   triangle-mesh shadow + crisp contact line + caustic pools + reverted
   opaque backing + sun-true light + rim-stroke clip + flat-cut base +
   base-aligned yaw. History of failures & fixes is in commit messages
   `8661129..7d0b00a` and code comments. Owner's outstanding verdicts:
   liked v3 seam, still dislikes residual hard edges. Diagnostic tooling:
   `example/test/crystal/render_scene_tool.dart` (renders PNG scene;
   `AmethystChunkPainter.debugGroundOnly` + debug outline taps),
   `amethyst_grounding_test.dart` (pixel density tests).
2. **Shadow from-scratch competition** (owner-requested): two worktrees
   under the session scratchpad `wt-codex` (branch `shadow-codex`, Codex
   exec running headless) and `wt-agy` (branch `shadow-agy`, Antigravity
   `agy` CLI — launcher script `~/.local/bin/agy-shadow`). Shared brief:
   scratchpad `shadow_brief.md` (spec: original soft blob aesthetic, no
   light gap at silhouette, no hard edges, no additive banding, down-left
   lean, purple transmitted glow inside shadow, scale-relative). Each
   commits on its branch. NOTE: worktrees/symlinks live in /tmp — if
   wiped, `git worktree prune` in canvas repo and re-create.
   When done: render each (`flutter test test/crystal/render_scene_tool.dart`
   in each worktree's example/), show owner side-by-side, she picks; port
   winner to the real branch, delete worktrees + shadow branches.
3. **Background test batch** bi4hc9298: main-app analyze + suites for M4.
   If lost: rerun `flutter analyze` + `flutter test test/spatial/
   test/screens/ test/services/task_overdue_helper_test.dart
   test/providers/ test/services/task_service_test.dart
   test/services/task_service_canvas_test.dart` in pin_and_paper/, expect
   green (+known flutter_js Linux failures in date_parsing only), then
   push the branch.

## Known environment facts (hard-won today)

- Verify test runs with EXIT CODES, not grep chains — four red pushes
  happened via `cmd | grep pattern` chains returning 0.
- `flutter analyze` sporadically reports ~18k issues when the owner's
  `flutter run` is regenerating ephemeral files — rerun, it clears.
- Example `flutter pub get -q` sometimes lies "Failed to update packages";
  verbose rerun succeeds.
- Gemini CLI (`gemini`) is dead (Google killed free tier); use `agy`
  (Antigravity, v1.1.10, `-p` print mode). Codex CLI: `codex exec
  --skip-git-repo-check --full-auto - < brief` (reads stdin; `-i img` for
  images). User settings allow `Bash(codex exec *)`, `Bash(agy *)`.
- Pixel-truth loop for visuals: render PNG via the scene tool, Read the
  image, crop with ImageMagick `convert`, probe colors with
  `-format '%[pixel:p{x,y}]'`. Trust probes over eyeballs on dark browns.

## Next after in-flight resolves

- Push M4; owner runs Spatial View on desktop + Android (manual pass per
  plan §Verification: fresh install AND v12-upgrade install).
- Docs: append M4-done to plan Progress log; flip addendum status line.
- Queued follow-ups: back-fields settings UI, completed-card hiding pref,
  live canvas updates, Supabase canvas-column sync, MCP top-insert chip
  session (task_de147841, owner-side), sketchpad 27MB asset bloat,
  amethyst → real app as desk_objects feature, drag-feel candidates 2/3
  (tilt, settle) in canvas docs/drag-feel-research.md.

## 2026-08-03 (later): shadow saga RESOLVED
Owner picked Gemini base; chimera (softened + Claude contact line/caustic) committed+pushed as pin_and_paper_canvas 8a4a890. Competition branches preserved on origin (shadow-codex, shadow-agy); scratch worktrees removed. Remaining in flight: M4 test-fix agent (hanging canvas_screen placeholder test + 1 failure), then push pin-and-paper branch.

## 2026-08-03 (later still): completed tasks excluded from landing tray
Owner-requested: Spatial View tray no longer stacks completed unplaced tasks
(pin-and-paper `ca5063e` on the POC branch; placed completed cards still
render at their stored position). Partially covers the queued
"completed-card hiding pref" follow-up — a settings-backed pref for placed
completed cards remains open. Discovered in passing: toggleTaskCompletion
writes the full task map, so a stale Task object clobbers canvas_x/canvas_y
(e.g. drag in Spatial View → complete from list before provider reload).
Tracked as a chip session (task_ab0bc430).

## 2026-08-03 (final): M4 VERIFIED AND PUSHED
pin-and-paper branch: f8348e5 (Spatial View) + 7b461d7 (test fixes: tester.runAsync for real DB I/O in widget tests; poll fire-and-forget persists). Independently verified EXIT=0. POC M1-M4 complete across all three repos. Owner: run Spatial View (AppBar button) on desktop + Android; manual pass per plan Verification section.
