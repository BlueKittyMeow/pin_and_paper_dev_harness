# HANDOFF — 2026-08-04 — cold-start entry point

**NEXT UP (owner, end of 08-04 evening session): Track A desk-objects
TAB DRAWER work** (local builds, as usual). The drawer's first
resident is ready: the accepted dachshund sprite bundle (Track B).

**Fresh session: read this, then whichever track you're working.**
Predecessor log: `HANDOFF-2026-08-03.md` (the marathon). Owner: Lara
(she/her). Working rules are in auto-memory
(`collaboration-checkins-subagent-economics`): chitchat decisions,
run procedure; codex ~free for research/critique, Claude subagents
deliberate; watch `/home` disk.

## Track A: Pin & Paper app — CARD DRAWINGS COMPLETE

Branch `claude/drag-drop-canvas-mvp-cu6uoy` across pin-and-paper +
pin_and_paper_sketchpad + pin_and_paper_card_renderer (+ canvas repo
from earlier). ALL of `CARD_DRAWINGS_PLAN.md` shipped and verified
(305 tests, exit 0): users can draw on card fronts AND backs via a
modal editor (real card face backdrop, 3-layer toolbar, cross-layer
undo/redo, stylus-default + finger toggle), drawings persist in DB v14
`task_drawings` (sync-silent by design), render as cached vector
overlays, per-face show/hide via eye chip, grey glyph marks hidden ink.
Also shipped 08-03→04: done pile (10 recent completed, top-right),
face-scan toggles + keep, stale-Task canvas-clobber fix, tag-row clip
fix, completed-cards-never-on-desk.

**Owner device pass queue (everything needs her eyes, nothing blocks):**
M4 Spatial View desktop+Android (fresh + v12-upgrade install), done
pile, face toggles, drawing editor (pressure feel, chip ergonomics,
overlay crispness at zoom, stylus-only friction check), tag rows,
uncomplete-restores-position.

**Queued next (owner-approved directions, not yet built):** perspective
A/B prototype (whisper-skew vs none, desk plane only, chrome flat —
PERSPECTIVE_UI_RESEARCH.md); selection/multi-select + z-order project
(SELECTION_UX_RESEARCH.md + ZORDER_RESEARCH.md + DEFAULTS_TO_REVISIT.md
— toggle ON for Lara, tldraw overlap-aware reorder adopted); back-fields
settings UI (global, notes gets per-card toggle); amethyst chip-jump
fix + title/notes truncation reveal (see HANDOFF-08-03 polish list);
live canvas updates; Supabase sync for canvas+drawings (remote table
first); desk-objects drawer; manila-folder card workspace (opens to
~85% screen); time-dynamic gobo lighting (mood refs:
reference/lighting_mood_01-04.png).

## Track B: Blender workshop — RIG AUTHORED + NOTEBOOK PILOT SHIPPED

`~/Documents/Git/agentic-blender-props` — READ ITS CLAUDE.md FIRST,
then MCP_PILOT_NOTES.md (2026-08-04 session findings + the working
recipe for the dachshund).
Done 2026-08-04 (late session): camera rig authored + frozen into
config/camera_stops.json (ortho, 20° elevation cheat, real
turntable_deg per stop — mockup strip numbers kept as label_deg;
shared/rig.py rebuilds it; shared/camera_rig.blend committed).
Notebook pilot COMPLETE: scripted-primitives build
(assets/notebook/scripts/build_notebook.py), ribbon = swept mesh strip
w/ parallel-transport drape (the dachshund-coat rehearsal — see pilot
notes), 3 correction rounds (codex critic, plateau rule invoked,
accepted by literal-128px measurement), geometry verifier GREEN
(712 tris, manifold, desk contact), GLB round-trip pass, full 7-stop ×
3-layer × 3-size sprite bundle in assets/notebook/renders/final/.
NEEDS LARA'S EYES: cover color (deep blue proposed, from her mockup),
ribbon gold (#c4941a-family — her accent color), general vibe.
RENDER FARM LIVE: MarshLair GPU renders bundles over plain SSH in ~70s
(vs ~75min local CPU) — recipe in workshop MCP_PILOT_NOTES.md. Also
frozen there: world-scale policy (global 3000 px/m, no per-asset
auto-fit — owner directive via side channel).
Stack notes: MCP surface has NO save/render/export tools — hybrid
workflow is mandatory (headless scripts for rig/build/render/export;
MCP+GUI for inspection/organic corrections). Watch /home disk: was
100% FULL mid-session; freed 10.4G by purging pip + poetry download
caches (~/.cache/pip, ~/.cache/pypoetry/{artifacts,cache}).

**DACHSHUND v1 ACCEPTED (owner, late 08-04: "he looks perfect").**
App-ready bundle: `assets/dachshund/renders/final_v1/` (84 sprites +
manifest, reproducibility proven 0px-diff from tag
`dachshund-v1-approved` / checkpoint final_stylized.blend, rebuild via
`build_dachshund.py -- <out> --style stylized`). Manifest carries
**ppm_multiplier 2** (tiny-prop exception — app must display at 1/2
scale). Standing-cheat decision: **straight sprites, no distortion**
(A/B/C composite in renders/skew_cheat_abc.png) — grounding comes from
the baked 20° camera + soft shadow layers composited ~40% opacity;
any global skew belongs to the app-side card-perspective A/B; shadow-
shear is a candidate mechanism for time-of-day light. 24-round history
in reports/iteration_log.jsonl; **docs/RETROSPECTIVE_2026-08-04.md**
(Opus analysis) is REQUIRED READING before asset 3 — headline: use the
model as a geometry compiler not an eye; reference-traced station
tables ("bandsaw", bandsaw.py) beat adjective-driven correction by an
order of magnitude; MCP guided layer wedged, direct RPC (rpc.py) +
headless scripts are the toolchain. V2 realism branch (--style
realistic, bandsaw-driven) is mid-flight: body widths/top-down, eye
polish, carved coat remain.

## Infrastructure notes

- Special-permission AI CLI wrappers exist: codex-claude-sp /
  agy-claude-sp (~/.local/bin), gated by ~/.claude/hooks/sp-gate.sh
  (PreToolUse → forces ask per run). Created via Lara's approval
  prompts in manual mode. Plain codex: research via
  `codex exec -c tools.web_search=true --skip-git-repo-check --full-auto - < brief.md`.
- All research docs live in docs/working/: CARD_DRAWINGS_* (plan +
  codex + agy reviews), SELECTION_UX, ZORDER, PERSPECTIVE_UI,
  DESK_OBJECTS(+_3D_RESEARCH), DEFAULTS_TO_REVISIT, BLENDER brief.
- Owner mockups: reference/desk_mockup_cards_{flat,skewed}.png — the
  7-stop rotation strip in them is the sprite-set spec. The dog is a
  PLACEHOLDER; the real asset is a marble LONGHAIRED DACHSHUND.
